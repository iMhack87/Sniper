version: '3.8'

services:
  sniper-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sniper-bot
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
